# eDEX-OS 项目规则(永久生效)

本项目启用 **Harness Engineering 驾驭工程约束**。不依赖模型自觉,全部执行流程、权限边界、验收规则、终止条件均为刚性强制规则,优先级高于所有临时对话指令。

所有任务必须严格按本文规则执行,禁止省略、跳过、变通、简化流程。

## 一、核心执行范式(固定四阶段闭环,不可跳过)

所有开发、修改、重构、调试任务,强制四阶段执行,严禁直接改代码:

**阶段 1:纯调研(只读模式)**
- 仅读取、分析、梳理代码、逻辑、报错、项目结构
- 不做任何文件修改、删除、新增、命令变更
- 输出标准化内容:需求理解 + 涉及文件清单 + 问题根因 + 完整修改方案

**阶段 2:人工确认闸门(强制卡点)**
- 方案输出后,主动暂停任务,等待用户明确确认「可以开始实现」
- 未收到确认,绝不进入编码、修改、执行环节

**阶段 3:定点实现(最小改动原则)**
- 严格按照确认后的方案执行,禁止私自扩需求、改逻辑、新增无关代码
- 改动遵循最小侵入,不格式化全局代码、不批量修改无关文件

**阶段 4:刚性校验验收(不信任模型自评)**
- 修改完成后,必须自动执行项目内置校验:lint、类型检查、单元测试、构建编译
- 输出简明易懂的报告,禁止只口头说明「已修复、无问题」
- 存在报错、告警、构建失败,判定为任务未完成

## 二、Agent 循环约束(防无限死循环、瞎重试)

- 单任务最大迭代修复轮次:2 轮
- 两轮修复后仍报错/不达标,立刻停止迭代,汇总完整问题、日志、卡点,等待用户决策
- 禁止反复试错、盲目改代码、不断微调参数消耗上下文
- 任务偏离原始需求、出现越界改动,必须立刻主动预警并终止自主执行

## 三、权限边界与安全红线(永久禁止行为)

未经个别允许,绝对禁止执行以下操作:
- 禁止执行 `rm -rf`、强制删除批量文件、清空目录
- 禁止 `git push --force`、篡改 `.git` 目录、删除提交记录、强制覆盖分支
- 禁止修改系统配置、环境变量、用户隐私文件(`~/.ssh`、`/etc` 等系统路径)
- 禁止操作项目外文件、跨目录批量改动、访问隐私敏感路径
- 禁止擅自执行高危 shell 命令、端口监听、网络请求、脚本提权操作
- 禁止编造不存在的文件、函数、变量、路径(幻觉造假)

**高危操作强制确认规则**:批量文件删除、批量重命名、数据库变更、核心配置改动,必须单独弹窗申请人工确认,未允许不得执行。

## 四、上下文治理(防健忘、防信息爆炸)

- 禁止一次性全量加载超大仓库,仅按需加载当前任务相关文件
- 复杂多步骤任务优先使用子代理 `/agent` 拆分,子任务结束后清理冗余上下文
- 会话过长、日志冗余时,主动精简无效信息,保留核心规则与任务进度
- 本文档规则为永久最高优先级,无论对话多长、上下文多少,始终生效,不被覆盖

## 五、版本回滚安全机制(兜底 Harness)

- 任何代码批量修改、核心逻辑重构前,必须先创建 Git 临时 checkpoint 提交
- 出现改错、改崩、逻辑异常时,优先提供回滚方案,保障原始代码安全
- 重大改动优先使用独立分支/临时工作区测试,确认无误再落地主代码

## 六、输出规范与复盘要求

每完成一个任务,必须输出极简复盘总结:
- 本次改动文件清单
- 核心变更点
- 自测/构建结果
- 遗留风险与后续建议

## 七、终极 Harness 核心原则(永远生效)

1. 绝不信任模型自评:一切正确性以工具、日志、测试、构建结果为准
2. 绝不允许无约束自主迭代:所有循环有上限、所有高危操作有闸门
3. 规则大于灵感:无论模型认为方案多优,必须遵守流程与边界
4. 安全优先于效率:改错可控、可回滚、可复盘,优先于快速完成
