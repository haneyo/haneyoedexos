# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules
jspm_packages

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# JetBrains IDE project folder (e.g. WebStorm)
.idea

dist
prebuild-src
src/node_modules

# macOS
.DS_Store

# Local Claude config (machine-specific)
.claude/

# Secrets / API keys (never commit)
sk-*/

# Build artifacts
*.AppImage
eDEX-OS-*.iso


# --- local machine-specific / sensitive (never commit) ---
# settings.local.json holds the real LAN IP + SSH user when OpenAI's Claude /
# Claude Code session writes it; keep it and any local override out of git.
settings.local.json
*.local.json
# Environment files
.env
.env.*
.env.local
# Private/SSH keys
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
*.ring
.ssh/
# Backups / editor junk
*.bak
*.orig
*~
*.swp
# App runtime data (never commit a real settings.json with lockCode / apiKey)
settings.json
