# eDEX-OS 交接文档 — 2026-08-15

> 目的:用户重启电脑后,新会话凭此文档无缝续接。用户明确:重启后再下载 ISO。

## 当前版本状态
- **仓库版本:v2.4.8**(tag `v2.4.8` 已 push,commit `a130871`)— 含 CPU 满载修复(#92)
- **v2.4.7 已构建完成**:run `31869932509` release+iso 均 success(v2.4.7 ISO 已就绪,但重装**用 v2.4.8 ISO**更优:一次验证 firstboot + CPU 修复)
- **v2.4.8 CI 进行中**:run `31875250641`(Release eDEX-OS)。release job 完成 → 下载 AppImage 部署笔记本;iso job(~40min)产出 v2.4.8 ISO
- **笔记本运行:v2.4.6 + 热修 + 诊断冻结**:globe/CSS/cyber 绘制被 CDP 冻结(CPU 已降至 ~10%),cyber 面板绘制空白待 v2.4.8 恢复
  - 会话 mount:Uoc8vss(热修产物解压),appmonitor 干净绑 6080/9222
  - 备份:`.bak-246`(带 TERM 的 v2.4.6)、`.bak-244`、`.bak-89const`、`.pre89`

## 进行中:v2.4.8 CI(CPU 满载修复 #92)
- **Release eDEX-OS (AppImage + ISO)** workflow,run **`31875250641`**
- **下载 AppImage**:release 完成后 `gh run download 31875250641 -R haneyo/haneyoedexos -n <appimage-artifact> -D ~/Downloads`(asset 下载快;artifact 才限速)
- **下载 ISO**:`gh run download 31875250641 -R haneyo/haneyoedexos -n <iso-artifact> -D ~/Downloads`(4.9GB,**优先 OSS 直链**,release.yml 输出;OSS 仅自用,README 不公开链接)
- CPU 修复验收:笔记本瞬时 CPU(见下),熄屏/锁屏时 <15%

## 遗留待办(重启后)
1. **部署 v2.4.8 AppImage 到笔记本**(等 release job)→ 重启会话 → 验证:瞬时 CPU 可见 ~35%/熄屏 <15%、cyber 面板恢复绘制、tab 标签 EMPTY、SSH 正常
2. **下载 v2.4.8 ISO** → fresh 重装验证 **firstboot 修复**(装完能进 eDEX UI,不再黑屏)
3. **回退 edex-session.sh 调试 flag**:`--remote-debugging-port=9222`(备份在 `/usr/local/sbin/edex-session.sh.bak-cdp`),验证完成后回退
4. 备份清理(bak-246/bak-244 等,确认无误后删)
5. 任务状态:#58(carbonyl,基本完)/#72(firstboot,代码已推待真机验证)/#78(clash 按钮)

## 关键凭据
- 笔记本:`aki@192.168.6.160` 免密 SSH;root 密码 `5645`;锁屏密码 `5645`
- 笔记本用户 = aki,`/home/aki`(默认用户 home 路径见 memory default-user-home)

## 相关 memory
- `systemd-firstboot-ordering-cycle`:firstboot 必须 WantedBy=cloud-init.target(本次 ISO 修复点)
- `patch-expectout-minify-trap`:patch 幂等标记禁用 minify 变量名(本次 v2.4.7 根治点)
- `appmonitor-orphan-port-squat`:重启会话必须 pkill 清场,否则旧 appmonitor 占 6080/9222
- `version-bump-lockfile-trap`:bump 版本只精确 Edit 3 处

## 本轮改动(2026-08-15,commit c969e27)
- `packaging/patch-appimage.sh`:renderer target expectOut `"+a+" ssh.socket")` → `" ssh.socket"`(稳定特征串,防 fresh 构建误跑)
- `src/package.json` + `src/package-lock.json`:2.4.6 → 2.4.7(精确 3 处)
- 笔记本:热修撤销 tab1 TERM→EMPTY(undo19 脚本在 `/tmp/edex246/undo19.sh`,产物 `/tmp/edex246/rel/eDEX-UI.v246fix.AppImage`)
