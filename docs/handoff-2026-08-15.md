# eDEX-OS 交接文档 — 2026-08-15

> 目的:用户重启电脑后,新会话凭此文档无缝续接。用户明确:重启后再下载 ISO。

## 当前版本状态
- **仓库版本:v2.4.7**(tag `v2.4.7` 已 push,commit `c969e27`)
- **笔记本运行:v2.4.6 + 热修**(撤销 #19 误注入的 tab1 "TERM"→"EMPTY"),已验收通过
- 笔记本 `/opt/edex/eDEX-UI.AppImage` = 热修产物(185164550B,sha `2364ca930b...be3ab`)
  - 备份:`.bak-246`(带 TERM 的 v2.4.6)、`.bak-244`、`.bak-89const`、`.pre89`
  - 会话 mount:Uoc8vss(热修产物解压),appmonitor 干净绑 6080/9222

## 进行中:v2.4.7 CI 构建(重启后要查)
- **Release eDEX-OS (AppImage + ISO)** workflow,run **`31869932509`**
  - release job(~10min):构建 AppImage + patch + 挂 release asset
  - iso job(~40min):remaster Ubuntu 24.04 → `eDEX-OS-v2.4.7.iso`(~4.9GB artifact)+ OSS 镜像(成都 edex-os/edex-os/iso)
- 另一个 `Build packaged binaries` run `31869932510`(tag 触发的旧工作流,可忽略)
- **查询**:`gh run view 31869932509 -R haneyo/haneyoedexos --json status,conclusion,jobs`
- **下载 ISO**(重启后用户会指示):`gh run download 31869932509 -R haneyo/haneyoedexos -n <artifact> -D ~/Downloads`
  - 注意:GitHub artifact 下载限速 ~50KB/s(184MB 要 1h);ISO 4.9GB 走 artifact 会极慢。**优先 OSS 直链**(release.yml 会输出)或让用户在 OSS 控制台/本地下载。
  - memory:oss-mirror-personal(OSS 仅自用,README 不公开链接)

## 遗留待办(重启后)
1. **下载 v2.4.7 ISO**(用户明确重启后再做)→ fresh 重装验证 **firstboot 修复**(装完能进 eDEX UI,不再黑屏)
2. **回退 edex-session.sh 调试 flag**:`--remote-debugging-port=9222`(备份在 `/usr/local/sbin/edex-session.sh.bak-cdp`),验证完成后回退
3. 备份清理(bak-246/bak-244 等,确认无误后删)
4. 任务状态:#58(carbonyl,基本完)/#72(firstboot,代码已推待真机验证)/#78(clash 按钮)

## 关键凭据
- 笔记本:`aki@192.168.6.160` 免密 SSH;root 密码 `5645`;锁屏密码 `5645`
- 笔记本用户 = aki,`/home/aki`(默认用户 home 路径见 memory default-user-home)

## 相关 memory
- `systemd-firstboot-ordering-cycle`:firstboot 必须 WantedBy=cloud-init.target(本次 ISO 修复点)
- `patch-expectout-minify-trap`:patch 幂等标记禁用 minify 变量名(本次 v2.4.7 根治点)
- `appmonitor-orphan-port-squat`:重启会话必须 pkill 清场,否则旧 appmonitor 占 6080/9222
- `version-bump-lockfile-trap`:bump 版本只精确 Edit 3 处

## 本轮改动(2026-08-15,commit c969e27)
- `packaging/patch-appimage.sh`:renderer target expectOut `"+a+" ssh.socket")` → `" ssh.socket"`(稳定特征串,防 fresh 构建误跑)
- `src/package.json` + `src/package-lock.json`:2.4.6 → 2.4.7(精确 3 处)
- 笔记本:热修撤销 tab1 TERM→EMPTY(undo19 脚本在 `/tmp/edex246/undo19.sh`,产物 `/tmp/edex246/rel/eDEX-UI.v246fix.AppImage`)
